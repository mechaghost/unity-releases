.superpowers/
.worktrees/
node_modules/
.next/
tsconfig.tsbuildinfo
# Local Claude config is ignored, but shared repo skills are tracked.
.claude/*
!.claude/skills/
.env.local
.env.*.local
# Throwaway probe/inspection scripts (e.g. from review agents). Kept out of the
# repo root and the TS compile scope so a stray `git add -A` can't ship one.
__probe*.ts
__inv*.ts
