{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "npm run ingest:editor",
    "cronSchedule": "0 * * * *",
    "restartPolicyType": "NEVER"
  }
}
